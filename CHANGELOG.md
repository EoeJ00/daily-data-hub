# 更新日志

## v1.2.0 - 2026-09-02

### 修改内容

- 架上包读取改为先识别页签和表头，再只读取日期、投手、消耗、回流等实际需要的列，减少无关列和隐藏页签产生的 Google Sheets 请求与数据传输。
- 新增持久化映射计划：单表、多表匹配和架上包会保存已经验证的页签结构、表头位置及目标列映射，服务重启后可直接复用；配置、表头或页签变化时自动失效并重新解析。
- 单表归集改为日期窗口读取：先定位业务日期，再读取来源日期块和目标具体行，避免为一次预览下载整张大表。
- 多表匹配接入相同的日期窗口执行器，覆盖标准日报块、独立投手页签和大单元格结构，同时保持先写投手日报、复核后再写总表的顺序不变。
- 架上包归集按合并日期块读取来源数据，并对投手消耗表和总表执行精确行复核，降低大表预览与写入前校验耗时。
- 新增共享日期窗口与投影列合并工具；日期索引缺失、结构异常或窗口读取失败时自动回退原有完整投影读取，确保正确性优先。
- 保持原有业务规则不变，包括缺失日期自动补齐、总表只补日期、隐藏页签过滤、空值跳过、冲突保护、写入顺序及写后复核。
- 补充映射计划持久化、失效回退、千行大表精确窗口读取及三种业务场景回归测试。

### 注意事项

- 首次处理新工作簿或表格结构发生变化时仍会执行完整结构识别；验证完成后的后续运行才会使用映射计划快速通道。
- `data/mapping-plans.json` 是运行时缓存文件，不应提交到 Git；文件损坏或删除不会影响业务正确性，系统会回退解析并自动重建。

## v1.1.0 - 2026-08-31

### 修改内容

- 快照预览增加工作簿版本校验：未变化时复用已校验快照，发生变化时重新归集实时数据。
- 源表或目标表从正确值修改为错误值、从错误值修正为正确值后，下次预览均会重新识别并反馈最新结果。
- 归集期间工作簿再次变化时自动重采集一次，避免保存读取过程中的不一致结果。
- Google Drive 版本校验不可用时自动降级为 Google Sheets 实时读取；实时读取也失败时明确标记“数据未验证”，不再将旧快照显示为正常结果。
- 后台同步先校验工作簿版本，未变化时跳过完整归集，继续保留 Google 请求全局 1200ms 限速。
- 单表、多表匹配和架上包统一支持目标投手表及总表缺少日期时按顺序向下补齐日期行，再执行数值写入与复核。
- 前端增加“快照已校验”“已更新实时数据”“实时读取”“数据未验证”状态提示。
- 补充高层运行时架构图及快照校验、日期补齐相关回归测试。

### 注意事项

烟雾测试确认 Google Cloud 项目 `103001170705` 尚未启用 Drive API。启用前系统会安全降级为实时读取，正确性不受影响，但无法获得快照快速通道的性能收益。请在 [Google Cloud 控制台启用 Drive API](https://console.developers.google.com/apis/api/drive.googleapis.com/overview?project=103001170705)，启用后重启服务即可。Drive 文件的 `version` 会随服务端变更递增，适合用于本次新鲜度校验。
